# 進階工程計算機實作工作清單 (Scientific Calculator Task List)

- [x] 設計與實作 HTML 結構 (index.html)
  - [x] 載入 Outfit 或 Inter Google Fonts，並更新 `head`
  - [x] 設計雙行顯示螢幕（上行顯示公式、下行顯示當前輸入與結果）
  - [x] 新增狀態顯示欄（DEG/RAD、2nd 狀態）
  - [x] 實作 5 列的按鍵網格，新增 `2nd`、`deg/rad` 等科學鍵
  - [x] 新增滑入式歷史紀錄面板 (History Drawer)
- [x] 設計與實作 CSS 樣式 (styles.css)
  - [x] 設定全域變數（毛玻璃底色、漸層、陰影、霓虹光暈色系）
  - [x] 實作動態漸層背景與置中版面
  - [x] 實作毛玻璃卡片效果（backdrop-filter）
  - [x] 實作按鍵網格、不同類型按鍵（運算子、數字、功能鍵、等於鍵）的視覺區隔
  - [x] 實作按鍵 hover、active 微動畫與發光效果
  - [x] 實作歷史紀錄面板的滑入動畫與排版樣式
- [x] 實作 JavaScript 核心運算與互動邏輯 (script.js)
  - [x] 實作基礎狀態管理（DEG/RAD 模式、2nd 模式、表達式、歷史紀錄數組）
  - [x] 撰寫科學運算 Helper 函數（包含 `fact(n)` 階乘、以及角度/弧度轉換的 `sin`、`cos`、`tan` 等）
  - [x] 實作表達式轉換引擎：
    - [x] 移除空白，轉換 `×` 為 `*`，`÷` 為 `/`
    - [x] 實作隱式乘法轉換（例：`2π` -> `2*Math.PI`，`2(` -> `2*(`，`π(` -> `Math.PI*(` 等）
    - [x] 轉換常數 `π` -> `Math.PI`，`e` -> `Math.E`
    - [x] 轉換次方 `^` -> `**`
    - [x] 轉換 `√(` -> `sqrt(`，`³√(` -> `cbrt(`
    - [x] 轉換階乘 `!` 為 `fact(...)`
  - [x] 實作計算 `calculate()`，處理小數點精度並捕獲異常
  - [x] 實作按鍵點擊事件與 `2nd` 切換邏輯（點擊 `2nd` 時，按鈕文字與屬性會動態轉變為反三角函數/進階運算）
  - [x] 實作鍵盤快速鍵綁定（數字、基本運算、Enter、Backspace、Escape，以及科學函數如 `s` -> `sin(`, `c` -> `cos(`, `t` -> `tan(`, `l` -> `log(`, `n` -> `ln(`, `p` -> `π`, `^` -> `^`, `!` -> `!`）
  - [x] 實作歷史紀錄的讀取、儲存與面板呈現
  - [x] 加入詳細的中文註解說明
- [x] 手動驗證與調校
  - [x] 測試各項工程數學計算（如 `sin(30)` 於 DEG 模式下為 0.5）
  - [x] 測試隱式乘法與階乘相乘
  - [x] 測試鍵盤快速鍵功能
  - [x] 啟動本地開發伺服器展示成果
